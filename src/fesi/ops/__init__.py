"""Operations — scheduler, dashboard, journal CLI.

Sub-modules (Phase 1):
  scheduler.py  — APScheduler driving 5x/day scan jobs in UAE timezone
  dashboard.py  — Streamlit dashboard at localhost:8501
  journal.py    — CLI utilities for inspecting decisions, signals, outcomes
"""
